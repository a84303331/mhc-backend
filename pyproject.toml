[project]
name = "mhc-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hsiachisheng", email = "hsiachisheng@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "jinja2>=3.1.6",
    "python-dotenv>=1.2.2",
    "uvicorn>=0.51.0",
]

[project.scripts]
mhc-backend = "mhc_backend:main"

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
